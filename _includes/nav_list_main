<!--전체 글 수를 세기 위한 연산. sum 변수에 전체 글 수 저장-->

{% assign sum = site.posts | size %}

<nav class="nav__list">
  <input id="ac-toc" name="accordion-toc" type="checkbox" checked />
  <label for="ac-toc">{{ site.data.ui-text[site.locale].menu_label }}</label>
  <ul class="nav__items" id="category_tag_menu">
      <!--전체 글 수-->
      <li>
            🔥 <span style="font-family:'RIDIBatang';">전체 글 수</style> <span style="font-family:'RIDIBatang';">{{sum}}</style> <span style="font-family:'RIDIBatang';">개 🔥</style>
      </li>
      <li>
        <!--span 태그로 카테고리들을 크게 분류 ex) C/C++/C -->
        <span class="nav__sub-title">AI</span>
        <ul>
          {% assign ai_categories = "llm|paper|code|mlops|graph" | split: "|" %}
          {% for cat in ai_categories %}
            {% assign cat_posts = site.categories[cat] %}
            {% if cat_posts %}
              {% assign cat_label = cat %}
              {% case cat %}
              {% when "llm" %}
                {% assign cat_label = "LLM" %}
              {% when "paper" %}
                {% assign cat_label = "논문 리뷰(Review Papers)" %}
              {% when "code" %}
                {% assign cat_label = "코드 구현(Code Implementation)" %}
              {% when "mlops" %}
                {% assign cat_label = "MLOps" %}
              {% when "graph" %}
                {% assign cat_label = "그래프(Graph)" %}
              {% endcase %}
              <li><a href="/categories/{{ cat }}" class="">{{ cat_label }} ({{ cat_posts | size }})</a></li>
            {% endif %}
          {% endfor %}
        </ul>

        <span class="nav__sub-title">Data Engineering</span>
        <ul>
          {% assign data_categories = "SQL|hadoop|spark" | split: "|" %}
          {% for cat in data_categories %}
            {% assign cat_posts = site.categories[cat] %}
            {% if cat_posts %}
              {% assign cat_label = cat %}
              {% case cat %}
              {% when "SQL" %}
                {% assign cat_label = "관계형 데이터베이스(SQL)" %}
              {% when "hadoop" %}
                {% assign cat_label = "하둡(Hadoop)" %}
              {% when "spark" %}
                {% assign cat_label = "스파크(Spark)" %}
              {% endcase %}
              <li><a href="/categories/{{ cat }}" class="">{{ cat_label }} ({{ cat_posts | size }})</a></li>
            {% endif %}
          {% endfor %}
        </ul>

        <span class="nav__sub-title">DevOps & Infra</span>
        <ul>
          {% assign infra_categories = "linux|docker|git" | split: "|" %}
          {% for cat in infra_categories %}
            {% assign cat_posts = site.categories[cat] %}
            {% if cat_posts %}
              {% assign cat_label = cat %}
              {% case cat %}
              {% when "linux" %}
                {% assign cat_label = "리눅스(Linux)" %}
              {% when "docker" %}
                {% assign cat_label = "도커(Docker)" %}
              {% when "git" %}
                {% assign cat_label = "깃(GIT)" %}
              {% endcase %}
              <li><a href="/categories/{{ cat }}" class="">{{ cat_label }} ({{ cat_posts | size }})</a></li>
            {% endif %}
          {% endfor %}
        </ul>

        <span class="nav__sub-title">Study</span>
        <ul>
          {% assign study_pairs = "ho_ML:핸즈온 머신러닝|keras:케라스 창시자에게 배우는 딥러닝|cs231n:CS231n(CNN with DL)|diapp:데이터 중심 애플리케이션 설계|DS:분산 시스템|OS:운영 체제(OS)" | split: "|" %}
          {% for pair in study_pairs %}
            {% assign parts = pair | split: ":" %}
            {% assign cat_key = parts[0] %}
            {% assign cat_label = parts[1] %}
            {% assign cat_slug = cat_key %}
            {% assign cat_posts = site.categories[cat_key] %}
            {% if cat_key == "ho_ML" and (cat_posts == nil or cat_posts == empty) %}
              {% if site.categories["hands_on"] %}
                {% assign cat_posts = site.categories["hands_on"] %}
                {% assign cat_slug = "hands_on" %}
              {% endif %}
            {% endif %}
            {% if cat_posts %}
              <li><a href="/categories/{{ cat_slug }}" class="">{{ cat_label }} ({{ cat_posts | size }})</a></li>
            {% endif %}
          {% endfor %}
        </ul>

        <span class="nav__sub-title">Programming</span>
        <ul>
          {% assign prog_categories = "python|java|algorithm|web" | split: "|" %}
          {% for cat in prog_categories %}
            {% assign cat_posts = site.categories[cat] %}
            {% if cat_posts %}
              {% assign cat_label = cat %}
              {% case cat %}
              {% when "python" %}
                {% assign cat_label = "파이썬(PYTHON)" %}
              {% when "java" %}
                {% assign cat_label = "자바(JAVA)" %}
              {% when "algorithm" %}
                {% assign cat_label = "알고리즘(Algorithm)" %}
              {% when "web" %}
                {% assign cat_label = "웹(WEB)" %}
              {% endcase %}
              <li><a href="/categories/{{ cat }}" class="">{{ cat_label }} ({{ cat_posts | size }})</a></li>
            {% endif %}
          {% endfor %}
        </ul>

        <span class="nav__sub-title">Troubleshooting & Tips</span>
        <ul>
          {% assign support_categories = "error|tips" | split: "|" %}
          {% for cat in support_categories %}
            {% assign cat_posts = site.categories[cat] %}
            {% if cat_posts %}
              {% assign cat_label = cat %}
              {% case cat %}
              {% when "error" %}
                {% assign cat_label = "에러 해결 방법" %}
              {% when "tips" %}
                {% assign cat_label = "기타 팁들(설치, 방법,...)" %}
              {% endcase %}
              <li><a href="/categories/{{ cat }}" class="">{{ cat_label }} ({{ cat_posts | size }})</a></li>
            {% endif %}
          {% endfor %}
        </ul>

        <span class="nav__sub-title">자격증</span>
        <ul>
          {% assign cert_categories = "R_ML|survey_a" | split: "|" %}
          {% for cat in cert_categories %}
            {% assign cat_posts = site.categories[cat] %}
            {% if cat_posts %}
              {% assign cat_label = cat %}
              {% case cat %}
              {% when "R_ML" %}
                {% assign cat_label = "빅데이터 분석기사(R)" %}
              {% when "survey_a" %}
                {% assign cat_label = "사회조사분석사 2급" %}
              {% endcase %}
              <li><a href="/categories/{{ cat }}" class="">{{ cat_label }} ({{ cat_posts | size }})</a></li>
            {% endif %}
          {% endfor %}
        </ul>

        <span class="nav__sub-title">GIT BLOG</span>
        <ul>
          {% assign blog_categories = "md|git_blog" | split: "|" %}
          {% for cat in blog_categories %}
            {% assign cat_posts = site.categories[cat] %}
            {% if cat_posts %}
              {% assign cat_label = cat %}
              {% case cat %}
              {% when "md" %}
                {% assign cat_label = "마크다운(Markdown)" %}
              {% when "git_blog" %}
                {% assign cat_label = "git 블로그(Github Pages)" %}
              {% endcase %}
              <li><a href="/categories/{{ cat }}" class="">{{ cat_label }} ({{ cat_posts | size }})</a></li>
            {% endif %}
          {% endfor %}
        </ul>
        
      </li>
  </ul>
</nav>
